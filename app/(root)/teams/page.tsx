import Layout from "@/components/Layout";
import React from "react";
import { Teams } from "./_components/Teams";

const page = () => {
	return (
		<Layout>
			<Teams />
		</Layout>
	);
};

export default page;
